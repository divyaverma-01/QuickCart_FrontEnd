import React from "react";
import AccountStatus from "@/components/Admin/Account/Status";

export const metadata = {
  title: "Account Status Page | NextCommerce Nextjs E-commerce template",
  description: "This is a Account Status Page for NextCommerce Template",
  // other metadata
};

const AccountStatusPage = () => {
  return (
    <main>
      <AccountStatus />
    </main>
  );
};

export default AccountStatusPage;
